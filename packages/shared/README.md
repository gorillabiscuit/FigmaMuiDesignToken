# packages/shared

Placeholder — fill in once Phase 2 bootstrap creates the actual shared package.

See `packages/shared/CLAUDE.md` for the platform-agnostic rules that the eventual code will follow.
